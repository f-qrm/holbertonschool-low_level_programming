#include "main.h"
/**
 * mul - multiplies two integers
 *@a: the first integer
 *@b: the second integer
 *
 *Return: the value of a * b
 */
int mul(int a, int b)
{
	int rsl = a * b;
	{
		return (a * b);
	}
}
